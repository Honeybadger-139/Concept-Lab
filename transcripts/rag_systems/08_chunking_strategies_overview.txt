Hello guys, in this video we're going to be looking at the different chunking strategies. So in the previous few videos, we were just using the character text splitter. Okay, so that is one of the most basic primitive ways of chunking a particular file. But when we're dealing with more complex documents like PDFs that could contain images and tables and you know um when it gets complicated that is not going to be enough. we will have to equip ourselves with more advanced forms of chunking so that we have the confidence in our own rack systems and build more robust you know systems that give accurate answers. So we already know the rag injection pipeline has these particular steps. So first is we load the documents could be PDFs, docx text and we convert that into text and then we do something called text chunking right we take long texts and we break it up into smaller pieces and then we use the embedding model we convert the text chunks into vector embeddings and then finally we store the vector embeddings and finally we take those vector embeddings and we store them in a vector database. So this is what we're going to be focusing on. Chunking is the critical second step. It determines how your content gets divided for retrieval. Your RAD system doesn't search entire documents. It searches chunks. So the final answer generation quality depends on those chunks. Bad chunking breaks everything downstream. Even perfect embeddings cannot fix poorly split content. So here is the problem with basic chunking. In our first few videos, we used the character text splitter. It just cuts text at fixed character counts. It is simple but crude. So let us look at an example. So we have chunk one and chunk two all belonging to the Tesla document. So let's see how the chunk one ends. So we have uh however production cost rose by 12% due to supply chain. You can see it was actually cut somewhere right in the middle and the chunk two it started somewhere right here. Right? Challenges and inflation. It's not really connected to each other at all. So there's a couple of problems. So the first one it got split mid-sentence. So we can see supply chain and challenges. The second problem was it broke the related concepts apart. They both talk about the same thing and they should ideally be in the same chunk. But what ended up happening was they got split apart. Context gets lost across chunks. And then finally poor retrieval quality. So here is why better chunking matters. Your retrieval and your answer generation is only as good as your chunks. Bad chunks is equal to bad answers. If the chunks are too small, it would lack context. If it is too large, it means too much noise. Hits embedding model or context window limits. Poor boundaries. It splits related information and no structural awareness. Ignores document format. So to prevent all of this we are going to be learning five chunking strategies. The first strategy is the character text splitter. This is what we implemented in our code earlier. So we can use custom separators. This is something that we have yet to see. And then it is still useful for simple uniform documents or when speed matters most. Then we have the recursive character tech splitter which is an upgrade from character splitter. This tries to split it natural boundaries like paragraphs, sentences, words. This is some intelligence that this character splitter does not have. It falls back gracefully if chunks are too big. It preserves more context than basic splitting. Let's look at the next one. Document specific splitting. This respects the document structure. Let's say if you're dealing with a PDF file, this is going to have awareness about pages, sections, and headers. If you're dealing with a different file like markdown file or a CSV file or uh you know a DOCX file or a PPT file. So basically each document type gets appropriate treatment. And the fourth one is semantic splitting. It uses embeddings to detect topic shifts. It keeps the related concepts together. It splits when meaning changes, not just by size. It is more intelligent than the previous ones, but computationally very expensive. Let's look at the last one. Agentic splitting or AI powered chunking. The LLM itself analyzes content and decides optimal splits. It can understand complex relationships. It adopts to content type automatically. And then it is by far the most sophisticated but slowest or the most expensive because an LLM is involved. So in the next few videos we are going to be looking at each one of these really short and crisp and you are going to have confidence that you've never had before with dealing with chunking and rack systems. So I will see you in the next